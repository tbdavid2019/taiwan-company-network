{
  "openapi": "3.1.0",
  "info": {
    "title": "Taiwan Company Network Static GET API",
    "version": "1.0.0",
    "description": "Public read-only static JSON API. Relationship links are not independently verified ownership claims."
  },
  "servers": [
    { "url": "https://tbdavid2019.github.io/taiwan-company-network/api/v1" }
  ],
  "paths": {
    "/index.json": {
      "get": {
        "operationId": "getApiIndex",
        "summary": "Get API instructions and company shard routing",
        "responses": { "200": { "description": "Shard index" } }
      }
    },
    "/aliases.json": {
      "get": {
        "operationId": "getCompanyAliases",
        "summary": "Get TWSE and TPEx stock-code aliases",
        "responses": { "200": { "description": "Stock-code aliases" } }
      }
    },
    "/companies/{shard}.json": {
      "get": {
        "operationId": "getCompanyShard",
        "summary": "Get companies whose registered names share a first Unicode character",
        "parameters": [
          {
            "name": "shard",
            "in": "path",
            "required": true,
            "description": "Shard identifier obtained from index.json; do not guess it.",
            "schema": { "type": "string" }
          }
        ],
        "responses": { "200": { "description": "Company records and direct relationships" } }
      }
    }
  }
}
